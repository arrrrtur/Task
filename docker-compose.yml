version: "3.8"

services:
  app:
    build: ./
    command: ./app
    ports:
      - 8080:8080
    depends_on:
      - db


  db:
    restart: always
    image: postgres:latest
    volumes:
      - ./.data